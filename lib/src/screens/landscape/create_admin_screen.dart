import "package:flutter/material.dart";

class LandscapeCreateAdminScreen extends StatefulWidget {
  const LandscapeCreateAdminScreen({Key? key,}) : super(key: key);

  @override
  State <LandscapeCreateAdminScreen> createState() => _LandscapeCreateAdminScreenState();
}

class _LandscapeCreateAdminScreenState extends State<LandscapeCreateAdminScreen> {

  @override
  Widget build(BuildContext context) {
    double scnW = MediaQuery.of(context).size.width / 1000;
    double scnH = MediaQuery.of(context).size.height / 1000;
    return Container();
  }
}